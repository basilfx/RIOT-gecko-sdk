name: Check

on:
  push:
    branches:
      - master

  pull_request:

  workflow_dispatch:

jobs:
  check:
    name: Verify distribution
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run check script
        run: ./check.sh
